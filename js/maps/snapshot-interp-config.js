/** @see docs/topics/snapshot-interp/SPEC.md */
export const SNAPSHOT_INTERP_CONFIG = {
  /** remote authority moves with this speed */
  authSpeed: 140,
  /** snapshot send interval (ms) */
  snapIntervalMs: 100,
  /** render delay behind latest received (ms) */
  defaultInterpDelayMs: 100,
  defaultLatencyMs: 80,
};
