// デモ: algorithms/boids.html
// separation / alignment / cohesion
public static class Boids
{
    // 各 boid について近傍を集め:
    // sep: 近傍から離れる平均方向
    // ali: 近傍速度の平均
    // coh: 近傍重心へ向かう
    // force = wS*sep + wA*ali + wC*coh; clamp(maxForce)
}
