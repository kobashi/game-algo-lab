// デモ: algorithms/obstacle-avoidance-nav.html
// 1) BFS path on grid  2) seek next waypoint  3) local avoid walls
public static class NavSteering
{
    // path = Bfs(start, goal)
    // desired = Seek(waypoint) + Avoid(nearby walls)
}
