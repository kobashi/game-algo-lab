# トピック仕様: モンテカルロ法

| 項目 | 値 |
|------|-----|
| id | `monte-carlo` |
| カテゴリ | game-tree |
| 状態 | implemented |
| 作成日 | 2026-07-17 |
| 依存 | Min-Max / α-β（「全部読む」の限界を知っているとよい） |

---

## 1. 学習目標

1. **プレイアウト**（根から葉まで乱択で進む）の平均で局面を評価する流れを追える  
2. Min-Max の「最善前提の値」と、乱択の**期待値**は一般に違うと説明できる  
3. 試行を増やすと推定が安定する（ばらつきが減る）様子を観察できる  
4. 次の多腕バンディット／MCTS へつながる「サンプルで決める」発想を掴む  

---

## 2. アルゴリズム（本デモ）

単純モンテカルロ評価（ランダムプレイアウト平均）:

```
Estimate(root, N):
  sum = 0
  for i in 1..N:
    n = root
    while n が葉でない:
      n = children(n) から一様ランダム
    sum += n.score
  return sum / N
```

- 乱数シード固定オプションで再現可能にする  
- 真の乱択期待値 EV と Min-Max 値を比較表示  

---

## 3. 初期木

Min-Max と同型の小さな木（`js/maps/monte-carlo-tree.js`）

- Min-Max 最善: 手 C、v=4  
- 乱択 EV: 各枝を等確率で進んだときの葉の平均  

---

## 4. 画面

- 木 + 今回のプレイアウト経路  
- 試行回数、推定値、履歴  
- 再生 = 連続プレイアウト / 1ステップ = 1 プレイアウト  

---

## 5. C#

`samples/MonteCarloExample.cs`
