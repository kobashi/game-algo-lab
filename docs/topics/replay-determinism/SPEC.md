# トピック仕様: リプレイと決定性

| 項目 | 値 |
|------|-----|
| id | `replay-determinism` |
| カテゴリ | quality |
| UI 型 | explain |
| 状態 | implemented |
| 成熟度 | oneshot |
| 作成日 | 2026-07-27 |
| 依存 | rng-seed, command-pattern, save-load |

## 学習目標
1. 同じシード + 同じ入力列なら同じ軌跡になると観察できる  
2. 入力をフレーム単位で記録し再生できる  
3. 乱数を決定的にしないとリプレイが壊れると分かる  

## 概要
簡易移動 + 乱数ジャンプ。記録 → 再生。シード固定/非固定を比較。
